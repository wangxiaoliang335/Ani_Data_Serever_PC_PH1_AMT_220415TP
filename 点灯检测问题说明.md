# 点灯检测问题分析报告

## 问题描述

点灯检软件在第二次检测时，未按照标准流程等待 MC 发送 `Start@` 命令就开始检测，导致 MC 侧无法正确处理检测结果。

---

## 正常检测流程

```
MC (本程序)                              点灯检软件
     |                                        |
     | 1. 发送 Start$01000000@               |
     | ─────────────────────────────────────→ |
     |                                        |
     | 2. 接收 Running@                       |
     | ←───────────────────────────────────── |
     |                                        |
     | 3. 接收 SnapFN@                        |
     | ←───────────────────────────────────── |
     |                                        |
     | 4. 接收 FN$01000000@                   |
     | ←───────────────────────────────────── |
     |                                        |
     | 5. 处理结果，写入 PLC / DFS / DB       |
```

---

## 异常现象

### 日志记录

| 时间 | 事件 | 状态 |
|------|------|------|
| 20:09:38 | 发送 Start@ | cycleInProgress=1, activeSlots=[1][0][0][0] |
| 20:09:42 | 接收 FN$ | 结果正常处理 ✅ |
| 20:12:59 | 接收 Running@ | **无 Start@ 日志** ❌ |
| 20:13:10 | 接收 FN$ | cycleInProgress=0, activeSlots=[0][0][0][0] |
| 20:13:10 | 处理结果 | fixtureNo=1 but slot not active (ignored) ❌ |

### 关键日志

```
20:12:59 Received: Running@        ← 没有先发送 Start@，点灯机自发开始检测
20:13:10 [Lighting] OnLightingResult STATE: cycleInProgress=0, running=1, snapDone=1, activeSlots=[0][0][0][0]
20:13:10 [Lighting] fixtureNo=1 but slot not active (ignored)
```

---

## 问题根因

点灯检软件**不等 MC 发送 `Start@` 命令就直接开始检测**。

正常情况下，点灯检软件应该：
1. 等待 MC 发送 `Start$...@` 命令
2. 收到 `Start@` 后才开始检测
3. 检测完成后发送 `Running@` → `SnapFN@` → `FN$...@`

但当前点灯检软件可能在以下情况会跳过等待：
- 收到 PLC 外部触发信号
- 自动循环检测模式
- 其他配置问题

---

## 影响分析

当 `activeSlots=[0][0][0][0]` 时，MC 会忽略本次检测结果，导致：

| 影响项 | 说明 |
|--------|------|
| PLC VisionResult | 保持上一次的值，不更新 |
| DFS 数据 | 检测结果未上传，追溯数据丢失 |
| 产品追溯 | 点灯检测数据不完整 |

---

## 排查方向

请点灯检软件人员检查以下几点：

### 1. 检测触发模式
- 是否配置为"等待 Start 命令"模式？
- 是否误开启了"自动循环检测"或"外部触发"模式？

### 2. Start 命令依赖
- 点灯检软件是否正确解析 `Start$xxxxxxxx$xxxxxxxx@` 命令？
- 是否存在跳过 Start 也能开始检测的配置？

### 3. 通信日志
- 请提供点灯检软件侧的通信日志
- 确认是否收到 MC 发送的 `Start@` 命令

### 4. 配置检查
- 确认点灯检软件的"启动模式"配置为 "Remote" 或 "Host"（等待上位机命令）
- 确认没有配置"本地触发"或"定时触发"

---

## 联系方式

如有疑问，请联系 MC 程序开发人员。

---

**报告生成时间**: 2026-04-01
